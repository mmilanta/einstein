> This repo has been written without the usage of any LLM or autocompleate models.

# Einstein

This projects contains an implementation of vector moltiplication based on the einstein notation.

The implementation also keeps track of the gradient, making it suitable for gradient descent algorithm. This has been demonstrated by training an handwritten digit classifier with MINST.

You can try it out here `https://mmilanta.github.io/mnist/`.
